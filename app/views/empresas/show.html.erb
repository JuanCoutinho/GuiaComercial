<p class="text-green-600 mb-4 text-center"><%= notice %></p>

<div class="bg-white rounded-lg shadow-lg p-8 max-w-lg mx-auto">
      <%= link_to "<", empresas_path, class: 'text-gray-600 hover:underline' %>
 
  <h2 class="text-2xl font-bold text-center mb-4">Detalhes da Empresa</h2>
  
  <%= render @empresa %>

  <div class="mt-6 flex justify-between items-center">
    <div>
      <%= link_to "Editar esta empresa", edit_empresa_path(@empresa), class: 'text-blue-600 hover:underline' %> 
    </div>
    
    <%= button_to "Excluir esta empresa", @empresa, method: :delete, data: { confirm: "Você tem certeza?" }, class: 'bg-red-600 text-white py-2 px-4 rounded-lg hover:bg-red-700 transition duration-200' %>
  </div>
</div>
